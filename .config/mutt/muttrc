source $HOME/.config/mutt/mail

alternative_order text/plain text/enriched text/html auto_view text/html

set header_cache            = $HOME/.cache/mutt/
set history_file            = $HOME/.cache/mutt_history
set save_history            = 8192
set history_remove_dups     = yes
set abort_nosubject         = yes
set beep                    = no
set date_format             = '%d.%m %H:%M'
set index_format            = '%-4C %d %Z %-40.40a %s'
set status_format           = '%f  %u/%m'
set attach_save_dir         = $HOME/temp/
set status_on_top           = yes
set sidebar_visible         = yes
set sidebar_divider_char    = │
set help                    = no
set mailcap_path            = $HOME/.config/mailcap
set sleep_time              = 0
set pager_stop              = yes

color normal black white
color indicator white black
color sidebar_divider black white
color attachment black white
color tilde black white
color header black white '.*'

bind attach h exit
bind attach l view-attach
bind generic G bottom-page
bind generic N search-opposite
bind generic g top-page
bind generic h exit
bind generic l select-entry
bind index J sidebar-next
bind index K sidebar-prev
bind index L sidebar-open
bind index l display-message
bind index r reply
bind pager G bottom
bind pager N search-opposite
bind pager g noop
bind pager h exit
bind pager j next-page
bind pager k previous-page

bind generic ! noop
bind generic ';' noop
bind generic * noop
bind generic 1 noop
bind generic 2 noop
bind generic 3 noop
bind generic 4 noop
bind generic 5 noop
bind generic 6 noop
bind generic 7 noop
bind generic 8 noop
bind generic 9 noop
bind generic <down> noop
bind generic <end> noop
bind generic <enter> noop
bind generic <esc>/ noop
bind generic <f1> noop
bind generic <home> noop
bind generic <keypadenter> noop
bind generic <left> noop
bind generic <pagedown> noop
bind generic <pageup> noop
bind generic <return> noop
bind generic <right> noop
bind generic <up> noop
bind generic = noop
bind generic > noop
bind generic H noop
bind generic L noop
bind generic M noop
bind generic Z noop
bind generic [ noop
bind generic \CL noop
bind generic ] noop
bind generic q noop
bind generic z noop
bind index $ noop
bind index % noop
bind index & noop
bind index '#' noop
bind index . noop
bind index <down> noop
bind index <enter> noop
bind index <esc><tab> noop
bind index <esc>C noop
bind index <esc>L noop
bind index <esc>P noop
bind index <esc>V noop
bind index <esc>W noop
bind index <esc>b noop
bind index <esc>c noop
bind index <esc>d noop
bind index <esc>e noop
bind index <esc>k noop
bind index <esc>l noop
bind index <esc>n noop
bind index <esc>p noop
bind index <esc>r noop
bind index <esc>s noop
bind index <esc>t noop
bind index <esc>u noop
bind index <esc>v noop
bind index <keypadenter> noop
bind index <return> noop
bind index <space> noop
bind index <tab> noop
bind index <up> noop
bind index @ noop
bind index A noop
bind index B noop
bind index C noop
bind index D noop
bind index F noop
bind index G noop
bind index J noop
bind index K noop
bind index L noop
bind index N noop
bind index O noop
bind index P noop
bind index Q noop
bind index R noop
bind index T noop
bind index U noop
bind index V noop
bind index W noop
bind index Y noop
bind index \CB noop
bind index \CD noop
bind index \CE noop
bind index \CF noop
bind index \CK noop
bind index \CN noop
bind index \CP noop
bind index \CR noop
bind index \CT noop
bind index \CU noop
bind index a noop
bind index b noop
bind index c noop
bind index e noop
bind index g noop
bind index h noop
bind index m noop
bind index o noop
bind index w noop
bind index x noop
bind index y noop
bind index | noop
bind index ~ noop
bind pager ! noop
bind pager $ noop
bind pager % noop
bind pager & noop
bind pager '#' noop
bind pager '\' noop
bind pager - noop
bind pager . noop
bind pager 1 noop
bind pager 2 noop
bind pager 3 noop
bind pager 4 noop
bind pager 5 noop
bind pager 6 noop
bind pager 7 noop
bind pager 8 noop
bind pager 9 noop
bind pager <down> noop
bind pager <end> noop
bind pager <enter> noop
bind pager <esc>/ noop
bind pager <esc>C noop
bind pager <esc>L noop
bind pager <esc>P noop
bind pager <esc>c noop
bind pager <esc>d noop
bind pager <esc>e noop
bind pager <esc>k noop
bind pager <esc>n noop
bind pager <esc>p noop
bind pager <esc>r noop
bind pager <esc>s noop
bind pager <esc>u noop
bind pager <backspace> noop
bind pager <f1> noop
bind pager <home> noop
bind pager <keypadenter> noop
bind pager <left> noop
bind pager <pagedown> noop
bind pager <pageup> noop
bind pager <return> noop
bind pager <right> noop
bind pager <space> noop
bind pager <tab> noop
bind pager <up> noop
bind pager @ noop
bind pager B noop
bind pager C noop
bind pager F noop
bind pager H noop
bind pager J noop
bind pager K noop
bind pager L noop
bind pager N noop
bind pager O noop
bind pager P noop
bind pager Q noop
bind pager R noop
bind pager S noop
bind pager T noop
bind pager V noop
bind pager W noop
bind pager Y noop
bind pager \CB noop
bind pager \CD noop
bind pager \CE noop
bind pager \CF noop
bind pager \CK noop
bind pager \CL noop
bind pager \CN noop
bind pager \CP noop
bind pager \CR noop
bind pager \CU noop
bind pager ^ noop
bind pager a noop
bind pager b noop
bind pager c noop
bind pager d noop
bind pager e noop
bind pager i noop
bind pager o noop
bind pager q noop
bind pager w noop
bind pager x noop
bind pager y noop
bind pager | noop
bind attach <enter> noop
bind attach <esc>P noop
bind attach <esc>e noop
bind attach <keypadenter> noop
bind attach <return> noop
bind attach L noop
bind attach T noop
bind attach \CB noop
bind attach \CE noop
bind attach \CF noop
bind attach \CK noop
bind attach b noop
bind attach g noop
bind attach m noop
bind attach v noop
bind attach | noop
